# 設定矛盾チェックシステム 使用ガイド

## 概要

このシステムは、`/write-story`などで執筆した短編小説が既存のヤヒロ世界設定と矛盾していないかを、**別のコンテキスト**でチェックします。

### なぜ別のコンテキストでチェックするのか？

同じ会話セッション内でチェックすると、以下の問題が発生します：
- **確証バイアス**: 自分が書いた内容を正しいと思い込む
- **コンテキスト汚染**: 執筆時の判断が検証時にも影響する
- **客観性の欠如**: 第三者的な視点でのチェックができない

別のMCPサーバー（OpenAI API）を使うことで、完全に独立したコンテキストで検証し、客観的な矛盾検出が可能になります。

## セットアップ（初回のみ）

### 1. OpenAI API Keyの設定

[.mcp.json](.mcp.json)ファイルの`OPENAI_API_KEY`と`OPENAI_MODEL`を設定してください：

```json
{
  "mcpServers": {
    "yahiro-consistency-check": {
      "env": {
        "OPENAI_API_KEY": "sk-proj-xxxxxxxxxxxxxxxx",  // ← ここを実際のKeyに変更
        "OPENAI_MODEL": "gpt-5-nano",  // 使用するモデル（下記参照）
        ...
      }
    }
  }
}
```

#### 利用可能なモデル

`OPENAI_MODEL`環境変数で使用するモデルを指定できます：

| モデル | 特徴 | 相対コスト | 推奨用途 |
|--------|------|-----------|----------|
| `gpt-5-nano` | **超低コスト・GPT-5** | ☆☆☆☆☆ | **日常的なチェック・大量チェック（デフォルト）** |
| `gpt-4o-mini` | 高速・低コスト | ★☆☆☆☆ | 日常的なチェック |
| `gpt-4o` | 高精度 | ★★★☆☆ | 重要な短編の精密チェック |
| `gpt-4-turbo` | バランス型 | ★★☆☆☆ | 中程度の精度 |
| `o1-preview` | 最高精度（推論特化） | ★★★★★ | 最終確認・公開前チェック |
| `o1-mini` | 高精度・低コスト | ★★☆☆☆ | バランス重視 |

**推奨設定**:
- 初回チェック・日常使用・228件一括チェック: **`gpt-5-nano`（デフォルト）** ← 最安
- 精度重視: `gpt-4o`
- 最終確認: `o1-preview`（コスト高・時間かかる）

**コスト比較（228件チェック時の概算）**:
- `gpt-5-nano`: 約$0.16（基準）
- `gpt-4o-mini`: 約$0.41（2.5倍）
- `gpt-4o`: 約$0.91（5.7倍）

**注意**: モデルを変更した後は、Claude Codeを再起動してください。

### 2. Claude Codeの再起動

設定ファイルを変更したら、Claude Codeを再起動してMCPサーバーを読み込みます。

## 使用方法

### 基本的なワークフロー

```
1. /write-story で短編を執筆
   ↓
2. MCPツール check_consistency で矛盾チェック
   ↓
3. 矛盾があれば修正して再チェック
   ↓
4. 問題なければ /add-story でファイル保存
```

### 例1: 隊商の短編をチェック

```
# 1. 短編を執筆
/write-story

# 2. 執筆後、同じセッションで：
「今書いた短編を check_consistency ツールでチェックしてください。
topics: ["隊商", "交易"]」

# 3. 結果を確認
- is_consistent: true なら問題なし
- inconsistencies がある場合は各項目を確認して修正

# 4. 修正後、再チェック
「修正した短編を再度 check_consistency でチェックしてください」
```

### 例2: 家畜・繊維の短編をチェック

```
/write-story

「今書いた短編を check_consistency ツールでチェックしてください。
topics: ["家畜", "繊維"]
category: "生業と技術継承"」
```

## MCPツールの詳細

### check_consistency

**パラメータ:**
- `story_text` (必須): チェック対象の短編全文
- `category` (オプション): 短編のカテゴリ
- `topics` (オプション): 主なトピック配列

**使用例:**
```
check_consistency({
  "story_text": "執筆した短編の全文...",
  "category": "生業と技術継承",
  "topics": ["家畜", "集落"]
})
```

**出力:**
```json
{
  "is_consistent": false,
  "inconsistencies": [
    {
      "severity": "major",
      "category": "terminology",
      "description": "「毛羊」という用語が使われていますが、設定では「綿毛獣」が正式名称です",
      "location": "「毛羊を飼育している集落では...」",
      "conflicting_setting": "集落の人々の生活.md: 「綿毛獣（わたけじゅう）」",
      "suggestion": "「毛羊」を「綿毛獣」に統一してください"
    }
  ],
  "summary": "用語の不一致が1件見つかりました。重大な矛盾はありません。",
  "checked_files": [
    "視点別設定分類.md",
    "ヤヒロ世界設定（総合版）.md",
    "集落の人々の生活.md"
  ]
}
```

### get_related_settings

トピックに関連する設定ファイルを確認できます。

```
get_related_settings({
  "topics": ["隊商", "海"]
})
```

**出力:**
```json
{
  "隊商": [
    "隊商システム総合.md",
    "交易品と経済.md",
    "集落システムと歴史.md"
  ],
  "海": [
    "海洋生態系の変化.md",
    "船舶技術と海洋知識.md"
  ]
}
```

### list_all_settings

利用可能なすべての設定ファイルをリスト表示します。

```
list_all_settings({})
```

## トピック別の推奨設定

| 短編のテーマ | 推奨topics | 主な参照設定 |
|------------|-----------|------------|
| 隊商の旅 | ["隊商", "交易"] | 隊商システム総合.md |
| 家畜の飼育 | ["家畜", "集落"] | 集落の人々の生活.md |
| 繊維作り | ["繊維", "家畜"] | 繊維と織物.md |
| 塩の交易 | ["塩", "交易", "隊商"] | 塩の生産と流通.md |
| 海の航海 | ["海", "船"] | 船舶技術と海洋知識.md |
| 遺跡探索 | ["遺跡", "技術"] | GM・作者向け完全設定資料.md |
| 集落生活 | ["集落"] | 集落の人々の生活.md |
| 物理現象 | ["物理"] | ヤヒロ球殻構造と各種数値設定.md |

## 重大度の理解

チェック結果の`severity`は以下の基準で判定されます：

### critical（重大）
- 世界観の根幹に関わる矛盾
- 物理法則の違反
- 歴史的事実の大きな齟齬
- 絶対に修正が必要

**例:**
- 「ヤヒロ球殻の直径が設定と10倍違う」
- 「2000年前の災害前に設定されていない技術が登場」

### major（重要）
- 社会システムの矛盾
- 主要な用語の不一致
- 登場人物の設定違反
- 修正を強く推奨

**例:**
- 「毛羊」と書くべきところを「綿毛獣」と書いている
- 隊商の組織体系が設定と異なる

### minor（軽微）
- 細かい表現のゆれ
- 些細な用語の不統一
- 余裕があれば修正

**例:**
- 「甲羅牛」を「カメ牛」と表記
- 距離の単位の軽微な誤差

## トラブルシューティング

### MCPツールが見つからない

1. `.mcp.json`が正しいパスにあるか確認
2. Claude Codeを再起動
3. `enableAllProjectMcpServers: true`が設定されているか確認

### OpenAI APIエラー

1. API Keyが正しく設定されているか確認
2. APIクォータが残っているか確認（[OpenAI Dashboard](https://platform.openai.com/usage)）
3. ネットワーク接続を確認

### 設定ファイルが見つからない

1. `YAHIRO_SETTINGS_PATH`が正しいか確認（`.mcp.json`内）
2. ファイル名が正確か確認（日本語、全角・半角に注意）

### チェック結果がおかしい

1. `topics`を適切に指定しているか確認
2. 複数のトピックが関連する場合は配列で指定
3. GPT-4oの判断ミスの可能性もあるため、最終判断は人間が行う

## ベストプラクティス

### ✅ 推奨

- 短編執筆直後、保存前にチェック
- 関連トピックを適切に指定
- critical/majorの矛盾は必ず修正
- 複数回の修正・チェックを繰り返す

### ❌ 非推奨

- チェックせずに保存
- トピック未指定（デフォルトは集落のみ）
- minorな指摘を無視して積み重ねる
- 同じセッション内で執筆とチェックを完結させる

## 今後の拡張

将来的に以下の機能追加を検討：
- 用語辞書の自動更新
- 過去の短編との矛盾チェック
- キャラクター一貫性チェック
- 時系列の整合性チェック
